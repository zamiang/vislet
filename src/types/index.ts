/**
 * Shared type declarations for migrated visualizations.
 *
 * Chart and data shapes (e.g. the brooklyn `Sale` model, aggregation outputs)
 * are added here as components are ported in Phase 2.1.
 */

export {};
